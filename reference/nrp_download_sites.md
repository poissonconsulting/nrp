# Download CTD site table

Download CTD site table

## Usage

``` r
nrp_download_sites(db_path = getOption("nrp.db_path", file.choose()))
```

## Arguments

- db_path:

  The SQLite connection object or path to the SQLite database

## Value

site table
