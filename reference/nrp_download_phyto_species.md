# Download phytoplankton species table

Download phytoplankton species table

## Usage

``` r
nrp_download_phyto_species(db_path = getOption("nrp.db_path", file.choose()))
```

## Arguments

- db_path:

  The SQLite connection object or path to the SQLite database

## Value

The phytoplankton species table
