# Download CTD visit table

Download CTD visit table

## Usage

``` r
nrp_download_ctd_visit(db_path = getOption("nrp.db_path", file.choose()))
```

## Arguments

- db_path:

  The SQLite connection object or path to the SQLite database

## Value

CTD visit table
