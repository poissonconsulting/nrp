# A named character vector with the correct column names, data types, and order for raw phytoplankton data

A named character vector with the correct column names, data types, and
order for raw phytoplankton data

## Usage

``` r
phyto_input_cols
```

## Format

A character vector:

- FileName:

  This column is added automatically when data is read in with
  `nrp_read_phyto_file`
